# --- Secrets (never commit) ---
.env
.env.*
!.env.example

# --- Local-only strategy & research (deliberately not published) ---
APERTURE_BUILD_GUIDE.md
STRK20_WINNING_STRATEGY_REPORT.md
CLAUDE_CODE_MASTER_PROMPT*.md
docs/COMPETITIVE_NOTES.md

# --- Dependencies / build output ---
node_modules/
.next/
out/
dist/
build/
target/
.snfoundry_cache/
*.tsbuildinfo

# --- Tooling / OS ---
# Vendored third-party agent skills. Reinstall from skills-lock.json with
# `npx skills add starkience/strk20-agent-skills`.
.agents/
.DS_Store
.claude/
.playwright-mcp/
.vercel
*.log
npm-debug.log*
yarn-error.log*
.pnpm-debug.log*
coverage/

# Local-only working notes
TELEGRAM_QUESTIONS.md

# Next.js static export output (built and deployed, not committed)
apps/web/out/

# Payout preimages. The only thing that can open an escrowed payout; never commit.
.payouts/
